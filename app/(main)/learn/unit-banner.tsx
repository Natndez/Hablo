type Props = {
    title: string;
    description: string;
}

// Banner that each unit comp will have
export const UnitBanner = ({ title, description}: Props) => {
    return (
        <div></div>
    );
}